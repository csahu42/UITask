<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.csahu42.uitask">

  <application
      android:name=".UIApp"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity
        android:name=".ui.activity.FeedActivity"
        android:label="@string/feed"
        android:theme="@style/FeedStyle">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity
        android:name=".ui.activity.ProfileOneActivity"
        android:label="@string/profile"
        android:parentActivityName=".ui.activity.FeedActivity"
        android:theme="@style/ProfilePinkStyle"/>
    <activity android:name=".ui.activity.ProfileMusicActivity">
    </activity>
  </application>

</manifest>